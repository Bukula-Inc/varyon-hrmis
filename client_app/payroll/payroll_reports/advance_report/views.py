from django.shortcuts import render

# Create your views here.
from django.shortcuts import render

# Create your views here.
def advance_report(request):
    return render(request, 'advance_report/dashboard.html')