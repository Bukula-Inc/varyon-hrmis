from django.shortcuts import render

# Create your views here.
from django.shortcuts import render

# Create your views here.
def nhima_report(request):
    return render(request, 'nhima_report/dashboard.html')