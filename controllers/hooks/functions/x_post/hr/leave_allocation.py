# from controllers.utils.dates import Dates
# from controllers.utils import Utils
# from controllers.utils.object_generator import Generate_Object
# from controllers.core_functions.hr import Core_Hr
# # from 


# utils = Utils()
# dates = Dates()

   

# def leave_entry_deleting(dbms, object):
#     # core = Core_Hr(dbms, user=object.user, obj=object)
   
#     id= object.body.data.leid
#     doc = object
#     leave_allocation = dbms.get_doc (model="Leave_Allocation",name=doc,user=object.user)
#     # leave_allocation = dbms.get_doc (model="Leave_Allocation",name=id,user=object.user)
#     # if leave_allocation.get ("status") == utils.ok:
#     #     obj = Generate_Object (leave_allocation)
#     # leave_days = core.get_employee_leave_days (leave_allocation['employee'])
#     # leave_entries = core.get_list ("Leave_Entry",)
#     #   
#     utils.pretty_print(doc)
#     # docs = object.doc
#     # for doc in docs:
#     #     core = Core_Hr(dbms, user=object.user, obj=object)
        
       
#     #     leave_entries = core.get_list ("Leave_Entry", filters={"employee": leave_allocation['employee'], "leave_type": leave_allocation['leave_type'], "employee_name": leave_allocation['employee_name']})
#     #   
